// Pure function for routing logic
// export function getNextRoute(session: any, profile: any) {
//   if (!session) return "/(auth)";
//   if (profile?.isComplete === false) return "/(new-user)/PartTwo/step-4.0";
//   return "/(app)/(tabs)";
// }
