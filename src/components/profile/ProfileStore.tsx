import React from "react";
import { View } from "react-native";
import AppText from "../ui/AppText";

export default function ProfileStore() {
   return (
      <View>
         <AppText>ProfileStore</AppText>
      </View>
   );
}
